---
title: "CTT Assignment"
author: "Bruce Mallory"
date: "10/5/2021"
output: pdf_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(readxl)
library(psych)
```

#5. initial alpha() call
```{r comment=""}
mt <- read_excel("Motivations to Teach_Responses.xlsx", sheet = "Reverse Coded")
alpha(mt)
```
#6. alpha() call with "check.keys=TRUE"
```{r comment=""}
alpha(mt, check.keys=TRUE)
```

```{r}
keep <- c(1,3,4,6,8,10,11,12,14,15,16,17,18,20,21,23)
alpha(mt[,keep], check.keys = TRUE)
```

